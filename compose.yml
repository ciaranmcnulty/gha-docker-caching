services:
  php:
    image: "ciaranmcnulty/gha-docker-caching"
    build:
      context: "."
      x-bake:
        cache-to:
          - "type=gha,scope=php,mode=max,ignore-error=false"
        cache-from:
            - "type=gha,scope=php"
